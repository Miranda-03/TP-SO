#ifndef IO_ENUM
#define IO_ENUM

/**
* @brief enum para los 4 tipos de interfaces.
* @param GENERICA 
* @param STDIN 
* @param STDOUT
* @param DIALFS
*/
typedef enum {
    GENERICA,
    STDIN,
    STDOUT,
    DIALFS
} TipoInterfaz;

#endif