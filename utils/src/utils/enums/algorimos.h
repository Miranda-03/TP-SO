#ifndef ALGORITMO_ENUM
#define ALGORITMO_ENUM

/**
* @brief enum para los 3 tipos de algoritmo.
* @param FIFO 
* @param RR
* @param VRR
*/
typedef enum {
    FIFO,
    RR,
    VRR
} Algoritmo;

#endif