#ifndef MODULO_ENUM
#define MODULO_ENUM

/**
* @brief enum para los 4 tipos de interfaces.
* @param KERNEL 
* @param MEMORIA 
* @param IO
* @param CPU
*/
typedef enum {
    KERNEL,
    MEMORIA,
    IO,
    CPU
} TipoModulo;

#endif