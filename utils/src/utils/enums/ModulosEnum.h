#ifndef MODULO_ENUM
#define MODULO_ENUM

/**
 * @brief enum para los 4 tipos de interfaces.
 * @param MEMORIA
 * @param KERNEL
 * @param IO
 * @param CPU
 */
typedef enum
{
    MEMORIA,
    KERNEL,
    IO,
    CPU
} TipoModulo;

#endif